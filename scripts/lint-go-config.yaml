---

issues:
  exclude-use-default: false
  max-per-linter: 0
  max-same-issues: 0

linters:
  disable:
    - "prealloc"
    - "wsl"
  enable-all: true

linters-settings:
  dupl:
    threshold: 100
  errcheck:
    check-blank: true
    check-type-assertions: true
  gocyclo:
    min-complexity: 13
  govet:
    use-installed-packages: true
  lll:
    tab-width: 4
  maligned:
    suggest-new: true
  misspell:
    locale: "US"
  unparam:
    check-exported: true

...
